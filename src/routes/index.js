const jobs = require('./jobs'),
contracts = require('./contracts'),
admin = require('./admin'),
balances = require('./balances')

module.exports = {
    jobs,
    contracts,
    admin,
    balances,
}